<!DOCTYPE html>
<html><body><script>
navigator.geolocation.getCurrentPosition( 
  function(p){ 
    for(n in p.coords) { 
      document.write('<br\>' + n + ': ' + p.coords[n]); 
    }
  }/*,
  function(err){ 
    alert('<br\>Error: ' + err.message); 
  }, {
    enableHighAccuracy: true,
    timeout: 5000,
    maximumAge: 0
  }*/
);
</script></body></html>

